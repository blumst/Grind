version: '3.4'

services:
  webapp:
    image: grindsoft
    build:
      context: .
      dockerfile: Dockerfile  
    ports:
      - "8080:80" 
    environment:
      ASPNETCORE_ENVIRONMENT: "Development" 
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
